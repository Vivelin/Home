@model Vivelin.Home.ViewModels.HomeViewModel
@{
    ViewData["Title"] = "Home";
}

@if (Model.Quote != null)
{
    <figure class="quote" id="@Model.Quote.ID">
        <blockquote use-commonmark="true">
            @Html.Raw(Model.Quote.Text)
        </blockquote>
        <figcaption>
            @Html.Raw(Model.Quote.Citation)
        </figcaption>
    </figure>
}